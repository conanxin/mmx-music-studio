import type { IConfig } from '@tarojs/service'

const dev: IConfig = {
  apiBase: 'https://your-domain.example', // 占位符，Phase 3B 接入真实 server
  mode: 'mock',  // Phase 3A: mock only
}

export default dev