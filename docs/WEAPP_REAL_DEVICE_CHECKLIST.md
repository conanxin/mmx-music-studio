# 真机预览测试清单

> Phase 3D 文档 · mmx-music-studio
> 测试设备：Android / iOS 真机（微信开发者工具「真机调试」）
> 当前阶段：Server Mock，不真实生成，不消耗额度

---

## 首页（home）

- [ ] 页面正常打开，无白屏
- [ ] 页面标题「MiniMax 音乐创作台」显示正确
- [ ] 底部导航栏显示：创作 / 作品 / 模板 / 设置 / 文档
- [ ] 导航切换流畅
- [ ] 卡片无错位，适配 375px / 414px 宽度
- [ ] 「开始创作」按钮可点击
- [ ] 点击「开始创作」跳转到创作台

---

## 设置页（settings）

- [ ] 页面正常打开
- [ ] **API Base** 显示当前值（默认 `http://118.195.129.137:8787`）
- [ ] 点击「测试连接」按钮
- [ ] 连接成功显示 backend=mock
- [ ] realGenerationEnabled=false
- [ ] mockGenerationEnabled=true
- [ ] **Key 输入框**：占位符显示 `sk-...`
- [ ] 区域选择：cn / global
- [ ] **安全提示**：不要在不可信公开部署输入真实 key
- [ ] **音频能力**区块显示：
  - `wx.createInnerAudioContext` ✅
  - `wx.downloadFile` ✅
  - `wx.saveFile` ✅
  - HTTPS 警告（如未配置合法域名）

---

## 创作台（studio）

### 表单
- [ ] 模式 tab 横向滚动正常
- [ ] 4 个模式可切换：纯音乐 / 自动成歌 / 歌词成歌 / 参考改编
- [ ] 当前模式高亮
- [ ] **音乐描述**输入框可输入中文
- [ ] maxlength=500 限制生效
- [ ] **风格标签**横向排列
- [ ] 标签可选中/取消（高亮切换）
- [ ] 高级设置（如有）展开/收起正常

### 生成
- [ ] 点击「生成音乐」/「生成纯音乐」等按钮
- [ ] 进度条出现，4 步进度消息循环：
  1. 正在连接后端…
  2. 正在提交任务…
  3. 正在生成音乐（Server Mock）…
  4. 生成时间较长，请耐心等待…
- [ ] 底部提示「当前为 Server Mock，不消耗额度」
- [ ] 生成完成（Mock 约 10-20 秒）显示播放器卡片

### 生成后播放器
- [ ] 显示标题（来自 Mock 数据）
- [ ] 波形可视化占位显示
- [ ] 时长显示正确（如 3:24）
- [ ] **试听按钮**：
  - 播放状态显示 ⏸
  - 加载状态显示 ⏳
  - idle 状态显示 ▶
  - 无音频文件时显示「暂无音频文件」且按钮禁用
- [ ] **下载按钮**：
  - 点击后 toast「保存中…」
  - 保存成功 toast「保存成功」
  - 保存失败 toast「保存失败」
- [ ] **来源标签**：显示「Server Mock」或「本地 Mock」

### 最近作品
- [ ] 显示最近 3 条作品
- [ ] 显示标题、模式、时长

---

## 作品库（library）

- [ ] 页面加载时显示「正在加载作品…」
- [ ] 加载完成显示作品列表
- [ ] 显示数据来源：「Server Mock」或「本地 Mock」
- [ ] 每个作品卡片显示：
  - 标题
  - 模式标签
  - 来源标签
  - 时长
  - 相对时间（如「3 小时前」）
- [ ] **播放按钮**：
  - 有音频 → 可点击，状态切换正常
  - 无音频 → 禁用，显示「暂无音频文件」
  - 加载中 → ⏳
  - 播放中 → ⏸（同一首）| ▶（不同首）
- [ ] **下载按钮**：
  - 有音频 → 可点击
  - 无音频 → 禁用
  - 下载中 → ⏳
- [ ] 后端不可用时 fallback 到本地 Mock，显示提示文案

---

## 音频播放

### 基础功能
- [ ] iOS 端可播放（微信内置浏览器）
- [ ] Android 端可播放
- [ ] 播放时切到后台行为（暂停/继续）
- [ ] 播放完成自动停止
- [ ] 播放失败显示中文错误文案

### HTTP IP 访问（开发阶段）
- [ ] 模拟器可访问 `http://118.195.129.137:8787`
- [ ] 真机在关闭合法域名校验后可访问
- [ ] 切到 LTE/5G 能否访问（取决于网络策略）

### 网络
- [ ] Wi-Fi 环境正常
- [ ] 移动网络访问（开发阶段）
- [ ] 无网络时提示「网络连接不可用」

---

## 下载保存

- [ ] `wx.downloadFile` 调用成功
- [ ] `wx.saveFile` 调用成功
- [ ] 保存成功文件出现在「文件管理」
- [ ] 存储空间不足时提示
- [ ] `tempFilePath` fallback 正常工作

---

## 安全检查

- [ ] 小程序前端代码中无 API Key
- [ ] 无真实 Token / Secret 硬编码
- [ ] 无调用真实 MiniMax 生成接口
- [ ] 不消耗额度（后端返回 generationSource=mock）

---

## 微信开发者工具「真机调试」对比

| 项目 | 模拟器 | 真机调试 |
|------|--------|----------|
| HTTP IP 访问 | 正常（关闭校验） | 正常（关闭校验） |
| audio playback | HTML5 Audio | wx.createInnerAudioContext |
| download | - | wx.downloadFile |
| 性能 | 快 | 真实 |
| 网络环境 | 同电脑 | 手机网络 |

---

## 通过标准

全部 [ ] 项目勾选后，Phase 3D 真机预览通过。
